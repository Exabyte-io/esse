{
    "$id": "job",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "job schema",
    "allOf": [
        {
            "$ref": "job/base.json"
        }
    ],
    "properties": {
        "workflow": {
            "description": "Arch-workflow for the job",
            "$ref": "workflow.json"
        }
    },
    "required": [
        "workflow"
    ]
}
