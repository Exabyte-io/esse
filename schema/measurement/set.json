{
    "$id": "measurement/set",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "measurement set schema",
    "description": "A run: the measurements of one sitting, performed on one specimen (a sample set).",
    "type": "object",
    "allOf": [
        {
            "$ref": "../system/set.json"
        }
    ],
    "properties": {
        "_sampleSet": {
            "description": "The sample set (specimen) the run was performed on",
            "$ref": "../system/entity_reference.json"
        }
    }
}
