{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "espresso app schema",
    "type": "object",
    "properties": {
        "name": {
            "enum": [
                "vasp"
            ]
        },
        "summary": {
            "enum": [
                "VASP"
            ]
        },
        "version": {
            "enum": [
                "5.3.5"
            ]
        },
        "exec": {
            "enum": [
                "vasp"
            ]
        }
    }
}
