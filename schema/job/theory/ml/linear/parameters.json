{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "regression parameters schema",
    "properties": {
        "property": {
            "type": "string",
            "description": "name of the predicted or optimized characteristic"
        },
        "coefficients": {
            "type": "array",
            "description": "array of weights/coefficients of the function from linear or non-linear fit for each target in property vector"
        },
        "intercept": {
            "type": "array",
            "description": "list of intercepts (shift) from the linear or non-linear fit of data points"
        },
        "inputData": {
            "$ref": "file:input_data.json"
        }
    },
    "required": [
        "property",
        "coefficients",
        "intercept"
    ]
}
