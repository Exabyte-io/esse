{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Input schema for using variables from workflow scope",
    "type": "object",
    "properties": {
        "scope": {
            "description": "Scope of the variable. e.g. 'global' or 'flowchart_id'",
            "type": "string"
        },
        "name": {
            "description": "Name of the input data. e.g. ml_method_parameters",
            "type": "string"
        }
    },
    "required": [
        "scope",
        "name"
    ]
}
