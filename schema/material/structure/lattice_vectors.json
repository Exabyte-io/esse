{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "lattice explicit unit",
    "allOf": [
        {
            "$ref": "file:../../primitive/math/3d_vector_basis.json"
        }
    ],
    "properties": {
        "alat": {
            "description": "lattice parameter for fractional coordinates",
            "type": "number",
            "default": 1.0
        },
        "units": {
            "description": "units for lattice vectors",
            "type": "string",
            "enum": [
                "angstrom",
                "bohr",
                "alat"
            ]
        }
    }
}
