{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "convergence supercell constraints schema",
    "properties": {
        "cellSize": {
            "description": "notes whether the dimensions of the cell are allowed to relax",
            "type": "boolean"
        },
        "cellShape": {
            "description": "notes whether the angles between the vectors forming the cell are allowed to relax",
            "type": "boolean"
        },
        "atomicPositions": {
            "description": "notes positions of the atoms within the cell are allowed to relax",
            "type": "boolean"
        },
        "atomicConstraints": {
            "description": "object containing the various directional constraints placed on atom relaxations",
            "$ref": "file:atomic_constraints.json"
        }
    },
    "required": [
        "cellShape",
        "cellSize"
    ]
}
