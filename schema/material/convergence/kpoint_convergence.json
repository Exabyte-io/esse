{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "k point convergence study schema",
    "properties": {
        "electronics": {
            "description": "array of the individual electronic convergences that happen as part for each step in the convergence study",
            "type": "array",
            "items": {
                "$ref": "file:electronic.json"
            }
        },
        "parameter": {
            "description": "name of the input parameter that is adjusted within the job to find the converged total energy",
            "name": "string"
        },
        "property": {
            "description": "name of the property that is monited to determine convergence",
            "type": "string"
        },
        "criteria": {
            "description": "convergence criteria to be applied to the property being monitored for convergence",
            "type": "number"
        },
        "formula": {
            "description": "equation in python to evaluate whether the convergence criteria is reached",
            "type": "string"
        },
        "results": {
            "description": "Resulting properties extracted from the unit, besides the textual output.",
            "type": "array",
            "items": {
                "$ref": "file:../../job/model/method/workflow/unit/_result.json"
            }
        }
    },
    "required": [
        "electronic",
        "parameter",
        "property",
        "criteria",
        "formula",
        "results"
    ]
}
