{
    "schemaId": "model-unit",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "unit model reference schema",
    "type": "object",
    "properties": {
        "name": {
            "description": "Human-readable unit model name.",
            "type": "string"
        },
        "slug": {
            "description": "Unique unit model label used to read model data from look-up table.",
            "type": "string"
        },
        "head": {
            "description": "Whether this unit is the first one in the model sequence.",
            "type": "boolean"
        },
        "flowchartId": {
            "description": "Identity of the unit model. Used to trace the sequence of models.",
            "type": "string"
        },
        "next": {
            "description": "Next unit's modelId. If empty, the current unit is the last.",
            "type": "string"
        }
    },
    "required": [
        "slug",
        "flowchartId"
    ]
}
