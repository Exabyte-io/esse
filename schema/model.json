{
    "schemaId": "model",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "model schema (base)",
    "properties": {
        "units": {
            "description": "Contains the Units of the Model",
            "type": "array",
            "items": {
                "$ref": "model/unit.json"
            }
        }
    },
    "required": [
        "units"
    ]
}
