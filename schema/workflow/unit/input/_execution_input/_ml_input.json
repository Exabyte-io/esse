{
    "$id": "workflow/unit/input/-execution-input/-ml-input",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "execution unit input schema for ml engines",
    "description": "TODO: consider keeping executable `evaluate` and flavor `cross-validate` as before",
    "type": "object",
    "properties": {
        "nSplits": {
            "description": "number of groups to split the training dataset for cross-validation",
            "type": "number"
        },
        "targets": {
            "type":"array",
            "items": {
                "type": "string"
            }
        }
    }
}
