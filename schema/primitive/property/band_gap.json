{
    "schemaId": "primitive-property-band-gap",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "band gap schema",
    "allOf": [
        {
            "$ref": "../math/scalar.json"
        }
    ],
    "properties": {
        "kpointConduction": {
            "description": "kpoint inside conduction band in crystal coordinates",
            "$ref": "../math/point.json"
        },
        "kpointValence": {
            "description": "kpoint inside valence band in crystal coordinates",
            "$ref": "../math/point.json"
        },
        "type": {
            "type": "string",
            "enum": [
                "direct",
                "indirect"
            ]
        },
        "units": {
            "$ref": "../../units.json#/energy"
        }
    },
    "required": [
        "type"
    ]
}
