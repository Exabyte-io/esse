{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "unknown model schema",
    "description": "schema for cases when model is unknown",
    "properties": {
        "type": {
            "enum": [
                "unknown"
            ]
        },
        "subtype": {
            "enum": [
                "unknown"
            ]
        },
        "method": {
            "$ref": "../methods/unknown.json"
        }
    },
    "$id": "science-models-unknown"
}
