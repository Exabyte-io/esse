{
    "$id": "core/reusable/field-condition",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Field condition schema",
    "description": "Whether a switching-spectroscopy reading was taken with the bias applied (on, in-field) or after each pulse at zero bias (off, out-of-field)",
    "type": "string",
    "enum": [
        "on",
        "off"
    ]
}
