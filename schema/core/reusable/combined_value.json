{
    "$id": "core/reusable/combined-value",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Combined value schema",
    "description": "A value combined over repeated measurements: mean, standard deviation, count. Units come from the context the value appears in",
    "type": "object",
    "allOf": [
        {
            "$ref": "../primitive/scalar.json"
        }
    ],
    "properties": {
        "value": {
            "description": "mean over the combined measurements"
        },
        "standardDeviation": {
            "description": "population standard deviation over the combined measurements (not the standard error of the mean)",
            "type": "number"
        },
        "count": {
            "description": "number of measurements combined",
            "type": "integer"
        }
    },
    "required": [
        "standardDeviation",
        "count"
    ]
}
