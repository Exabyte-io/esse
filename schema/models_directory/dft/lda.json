{
    "schemaId": "models-directory-dft-definitions-lda",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Local Density Approximation (LDA) schema",
    "allOf": [
        {
            "$ref": "functional.json"
        }
    ],
    "properties": {
        "approximation": {
            "enum": [
                "LDA"
            ]
        },
        "name": {
            "$ref": "functional/definitions.json#/lda"
        },
        "components": {
            "type": "array",
            "items": {
                "$ref": "functional/components/local_functional.json"
            }
        }
    }
}
