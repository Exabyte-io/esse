{
    "schemaId": "models-directory-dft-definitions-gga",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "functional schema",
    "type": "object",
    "properties": {
        "approximation": {
            "description": "Approximation category, e.g. 'hybrid'.",
            "type": "string"
        },
        "name": {
            "description": "Name of functional used in calculation, e.g. BLYP",
            "type": "string"
        },
        "components": {
            "type": "array",
            "items": {
                "$ref": "functional/components.json"
            }
        },
        "dispersion_correction": {
            "$ref": "functional/components/dispersion_correction.json"
        }
    }
}
