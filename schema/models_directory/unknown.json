{
    "schemaId": "models-directory-unknown",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "unknown model schema",
    "description": "schema for cases when model is unknown",
    "allOf": [
        {
            "$ref": "unit_base.json"
        }
    ],
    "properties": {
        "type": {
            "enum": [
                "unknown"
            ]
        },
        "subtype": {
            "enum": [
                "unknown"
            ]
        },
        "method": {
            "description": "@todo: temporarily deactivated",
            "type": "object"
        }
    }
}
